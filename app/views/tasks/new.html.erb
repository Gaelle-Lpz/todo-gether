<%= simple_form_for [@project, @task] do |f| %>
  <%= f.input :title, disabled: false %>
  <%= f.input :detail, disabled: false %>
  <%= f.input :date, as: :date %>
  <%= f.button :submit %>
<% end %>
